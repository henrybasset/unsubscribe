# Runtime state — local to each machine, not source
seen.txt
log.txt

# Python
__pycache__/
*.pyc

# Build output (the signed release artifacts live on GitHub Releases, not in git)
*.dmg
*.dmg.sha256
*.dmg.asc

# macOS
.DS_Store
